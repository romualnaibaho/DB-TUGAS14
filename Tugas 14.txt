---

CREATE TRIGGER hitung_ipk_mahasiswa
BEFORE INSERT ON nilai_mahasiswa
FOR EACH ROW
BEGIN
INSERT INTO ipk_mahasiswa VALUES
(CONCAT(NEW.nama, ' (',NEW.nim,')'), ROUND((SUM(NEW.semester_1)+SUM(NEW.semester_2)+SUM(NEW.semester_3))/3, 2));
END$$